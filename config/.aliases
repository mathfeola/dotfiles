# Easier navigation: .., ..., ~ and -
alias ..="cd .."
alias cd..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."

# Shortcuts
# alias dl="cd ~/Downloads"
# alias blog="cd ~/Development/willianjusten.com.b  qr && code ."

# General aliases
alias update="gfa ; ggpull"

# iOS related
alias repoup="bundle exec pod repo update"
alias podi="bundle exec pod install"
alias bi="bundle install"
alias podiup="bundle exec pod install --repo-update"
alias rmderived="rm -rf ~/Library/Developer/Xcode/DerivedData
